import { writable } from "svelte/store";

export const catalogImageData = writable([]);
export const sizesApiData = writable([]);
export const colorsApiData = writable([]);
export const packingApiData = writable([]);